import { KeyValueType } from "../KeyValue.type";
/**
 * PubSubBulkPublishEntry defines an entry in a bulk publish request.
 */
export declare type PubSubBulkPublishEntry = {
    entryID: string;
    event: object | string;
    metadata: KeyValueType;
    contentType: string;
};
