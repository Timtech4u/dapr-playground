/**
 * BulkSubscribeConfig defines the configuration for a bulk subscription
 **/
export declare type BulkSubscribeConfig = {
    enabled: boolean;
    maxMessagesCount?: number;
    maxAwaitDurationMs?: number;
};
